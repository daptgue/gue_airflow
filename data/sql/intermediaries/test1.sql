CREATE OR REPLACE TABLE `{{ var.value.GCP_PROJECT }}`.`{{ var.value.DATASET_TESTING2 }}`.`dim_calender`
AS
SELECT  
    PERIOD_KEY,
    FULL_DATE,
    YYYYMMDD,
    WEEK_OF_MONTH,
    WEEK_OF_YEAR,
    MONTH_NUM,
    MONTH_NAME,
    MONTH_LONG_NAME,
    MONTH_END_FLAG,
    QUARTER_NUM,
    QUARTER_NAME,
    SEMESTER_NUM,
    SEMESTER_NAME,
    SEMESTER_LONG_NAME,
    YEAR_NUM,
    DAY_OF_WEEK,
    DAY_NAME,
    HOLIDAY
FROM `{{ var.value.GCP_PROJECT }}`.`.DWH_GPOSLITE.DIM_PERIOD`;